<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.groupenstein.groupenstein.SearchActivity">

    <EditText android:id="@+id/textSearch" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:hint="@string/prompt_search"
        android:imeActionId="@+id/login"
        android:imeActionLabel="@string/action_search_text"
        android:imeOptions="actionUnspecified" android:inputType="text"
        android:maxLines="1" android:singleLine="true" />

    <Button android:id="@+id/buttonSearch" style="?android:textAppearanceSmall"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="@string/action_search_button"
        android:textStyle="bold"
        android:background="@drawable/primarybutton"
        android:layout_marginTop="15dp"
        android:layout_below="@+id/textLocation"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/listSearchResults"
        android:layout_below="@+id/buttonSearch"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="31dp" />

    <EditText
        android:id="@+id/textLocation"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:hint="ZipCode or City and State"
        android:inputType="text"
        android:maxLines="1"
        android:singleLine="true"
        android:imeActionId="@+id/login"
        android:imeActionLabel="@string/action_search_text"
        android:layout_below="@+id/textSearch"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="15dp"/>

    <Spinner
        android:id="@+id/spinRadius"
        android:layout_width="110dp"
        android:layout_height="wrap_content"
        android:background="@android:drawable/btn_dropdown"
        android:spinnerMode="dropdown"
        android:layout_below="@+id/textSearch"
        android:layout_alignRight="@+id/textSearch"
        android:layout_alignEnd="@+id/textSearch"
        android:layout_above="@+id/buttonSearch"
        android:layout_marginTop="15dp"/>
</RelativeLayout>
